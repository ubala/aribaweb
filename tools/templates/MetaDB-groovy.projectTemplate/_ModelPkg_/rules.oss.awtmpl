/*
    @ModelPkg@ rules.oss -- Meta-data rules

    These are auto-loaded (lazily) when any component in this java package is
    referenced in the rules
*/

class=@ModelPkg@.@ModelClass@ {
    /* Put field or action rules (possibly scoped by operation, etc) here */
}
