<w:ModalPageWrapper title="$[]Choose File to Upload"
        okAction="$done" okTitle="$[]Cancel">
    <w:FileUploadChooser filename="$fileName"
        bytes="$bytes"
        mimeType="$mimeType"
        maxLength="$maxsize"
        fileSizeExceeded="$fileSizeExceeded"/>\
</w:ModalPageWrapper>